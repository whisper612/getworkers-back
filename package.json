{
  "name": "server",
  "version": "0.0.2",
  "description": "Getworkers API",
  "main": "server.js",
  "scripts": {
    "test": "\"No tests && exit 1\"",
    "start": "node server.js",
    "start-watch": "nodemon server.js",
    "dev": "concurrently  \"npm start && yarn run start-watch\" \"cd ../client && yarn start && yarn run start-watch\""
  },
  "author": "Joe GetWorkersDev",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "mysql": "^2.17.1"
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "nodemon": "^1.19.1"
  }
}
